/** Contains the title of a sample and its TeX code. */
export class Sample {
    /** Will be displayed as the title of a sample. */
    public name: string;

    /** The TeX code of a sample. */
    public code: string;
}
