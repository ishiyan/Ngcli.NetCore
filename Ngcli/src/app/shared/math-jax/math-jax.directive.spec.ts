import { MathJaxDirective } from './math-jax.directive';

describe('MathJaxDirective', () => {
  it('should create an instance', () => {
    const directive = new MathJaxDirective(null);
    expect(directive).toBeTruthy();
  });
});
